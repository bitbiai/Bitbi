/* ============================================================
   BITBI — Gallery image data (R2-backed public variants)
   ============================================================
   Single source of truth for public gallery items.
   ============================================================ */

export const galleryItems = [];
