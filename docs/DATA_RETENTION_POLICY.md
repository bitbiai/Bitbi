# Data Retention Policy Engineering Baseline

Date: 2026-04-26

Last reconciled: 2026-05-17

This file is an engineering baseline for retention and deletion behavior. It is not legal advice and does not claim GDPR/CCPA compliance. Product/legal review is required before enabling irreversible deletion or promising user-facing retention terms.

Current release truth: `config/release-compat.json` declares the latest auth D1 migration as `0056_add_ai_folder_image_ownership_metadata.sql`. Phase 4.19 adds `platform_budget_repair_actions` for explicit admin-approved repair audit rows; Phase 4.20 adds read-only bounded repair evidence reports/exports over those rows; Phase 4.21 adds sanitized evidence archive metadata plus `AUDIT_ARCHIVE` snapshots under `platform-budget-evidence/`. Phase 6.1 adds tenant asset ownership design/dry-run docs and scripts only, Phase 6.2 adds a focused `ai_folders`/`ai_images` owner-map dry run, Phase 6.3 adds schema/access planning, Phase 6.4 adds nullable ownership metadata columns for those two tables only, Phase 6.5 assigns those fields only on new personal folder/image writes, Phase 6.6 adds read-only dual-read diagnostics, Phase 6.7 adds admin-only evidence report/export over those diagnostics, Phase 6.8 adds evidence collection runbook/template/checklist docs only, Phase 6.9 adds a main-only evidence package with pending status because no real operator export was present in-repo, and Phase 6.10 adds the pending/blocked decision document. No automatic repair exists, reports/archives do not apply repairs, review-only actions do not mutate usage/source rows, and Phase 6.10 does not backfill ownership rows, assign organization ownership, move/list/delete R2 objects, change access checks, change lifecycle plans, generation behavior, gallery behavior, quota accounting, or billing. This policy baseline does not approve production deploy, full live billing readiness, full SaaS maturity, full tenant isolation, customer billing from platform caps, or full legal compliance.

## Principles

- Keep authentication secrets, session material, MFA material, reset tokens, service signatures, and provider credentials out of exports.
- Preserve security/audit evidence unless a reviewed retention policy explicitly allows deletion or anonymization.
- Prefer dry-run deletion plans and admin approval before any irreversible operation.
- Treat generated AI prompts, previews, media, wallet addresses, contact messages, and profile fields as potentially personal data.
- Do not inline large R2 binaries into export archives; Phase 1-I export archives use authorized JSON manifests with media references only and do not expose raw internal media R2 keys.
- Delete only owner-scoped or lifecycle-scoped R2 objects whose prefix has been explicitly approved. Phase 1-J cleanup is limited to `AUDIT_ARCHIVE` objects under `data-exports/`; Phase 4.21 platform budget evidence cleanup is limited to `AUDIT_ARCHIVE` objects under `platform-budget-evidence/`.
- Treat Admin Control Plane, Pricing page, and Credits dashboard data as presentation surfaces over existing backend records unless a future phase adds new storage. Phase 4.15.1 adds D1 app-level budget switch state/history for admin/platform AI controls only; it stores no Cloudflare API tokens or Cloudflare variable values. Phase 4.17 adds `platform_admin_lab_budget` cap limit and usage evidence only; Phase 4.18 adds read-only reconciliation evidence; Phase 4.19 adds explicit admin-approved repair action audit rows and can create missing platform budget usage evidence from local D1 source proof only; Phase 4.20 adds read-only repair evidence report/export over those existing rows; Phase 4.21 stores sanitized archive metadata and private archive objects for operator evidence only. These phases are not customer billing or live billing readiness.
- Treat Phase 6.1-6.9 tenant asset ownership output as design/dry-run/schema-planning plus new personal folder/image write metadata, read-diagnostics evidence, admin evidence report/export, evidence collection docs, and pending main-only evidence packaging only. It creates no old-row owner backfill, organization ownership assignment, R2 move/list/delete, lifecycle executor, access-check change, gallery change, quota change, or tenant isolation claim.

## Retention Candidates

| Data | Current behavior observed | Proposed retention | Deletion/anonymization action | Enforcement status |
| --- | --- | --- | --- | --- |
| Sessions | Cron cleanup exists for expired sessions. | Session TTL plus cleanup grace. | Revoke/delete on approved deletion. | Existing cleanup plus Phase 1-J safe executor pilot for approved requests. |
| Password reset tokens | Expiring auth tokens exist. | Short TTL only. | Expire/delete. | Existing cleanup plus Phase 1-J safe executor pilot for approved requests. |
| Email verification tokens | Expiring verification tokens exist. | Short TTL only. | Expire/delete. | Existing cleanup plus Phase 1-J safe executor pilot for approved requests. |
| SIWE challenges | Challenge table exists. | Short TTL only. | Expire/delete. | Existing cleanup plus Phase 1-J safe executor pilot for approved requests. |
| Admin MFA proofs/failed attempts | Durable failed-attempt state exists. | Short operational lockout window. | Expire/delete after lockout window. | Existing security state; explicit retention cleanup remains future work. |
| Admin MFA credentials/recovery codes | Stored for enrolled admins. | Retain while admin enrollment is active. | Revoke/delete only after admin continuity review. | Phase 1-H plans revoke action; executor deferred. |
| Profiles/avatars | Profile row and private R2 avatar. | Retain while account is active. | Delete/anonymize row; delete avatar object after approval. | Phase 1-H plan item only. |
| Wallet addresses | Linked wallet table. | Retain while linked/account active. | Delete/unlink. | Phase 1-H plan item only. |
| Favorites/folders | User-owned D1 rows. | Retain while account is active. | Delete. | Phase 1-H plan item only. |
| Generated AI images/text/audio/video | D1 metadata and `USER_IMAGES` R2 objects. | Retain while user keeps assets or product policy allows. | Delete D1 rows and R2 objects after approval. | Phase 1-H plan item only; executor deferred. |
| AI video jobs | D1 job state plus R2 output/poster references. Phase 4.5 adds bounded sanitized budget-policy metadata columns for admin async video jobs only. | Retain operationally while output is active; later archive or prune. | Delete/anonymize job metadata and R2 output after approval. | Phase 1-H plan item only. Budget metadata must not include raw prompts, provider request bodies, auth headers, cookies, Stripe data, Cloudflare tokens, private R2 keys, secrets, or raw request bodies. |
| AI video poison messages | D1 operational diagnostics. | Short to medium operational window. | Retain/redact; eventually purge. | Admin inspection exists; retention cleanup remains future work. |
| User activity logs | Hot table plus archive behavior. | Hot operational window plus archive policy. | Retain/anonymize for deletion requests. | Phase 1-G bounded hot-window search; Phase 1-H plan item. |
| Admin audit logs | Admin/security audit table and archive. | Security/legal retention window, likely longer than user content. | Retain or anonymize target identifiers after approved policy. | Phase 1-H plans retain/anonymize; no hard-delete. |
| Activity search projection | Derived indexed fields. | Same as source event. | Delete/anonymize with source event. | Projection cleanup exists for archive/prune; lifecycle executor deferred. |
| Contact form submissions | Sent through Resend; no repo-owned D1 table identified. | External processor retention policy required. | Manual processor workflow until repo-owned storage exists. | Not enforced by repo. |
| Export archives | Phase 1-I can generate bounded JSON archives into private `AUDIT_ARCHIVE` and records metadata in `data_export_archives`. Phase 1-J adds bounded cleanup. | 14 days for generated archives. | Expire archive access by metadata; delete only approved private `data-exports/` archive objects through bounded cleanup. | Generation, metadata, access-expiration, admin/scheduled cleanup, and cleanup-failure recording exist. |
| Platform budget evidence archives | Phase 4.21 can generate bounded sanitized JSON/Markdown archives into private `AUDIT_ARCHIVE` and records metadata in `platform_budget_evidence_archives`. | Default 90 days, bounded to 365 days by admin request. | Expire archive access by metadata; delete only approved private `platform-budget-evidence/` archive objects through bounded cleanup. | Archive creation applies no repair, mutates no usage/source/repair rows, calls no providers/Stripe/Cloudflare, and omits raw prompts/provider bodies/raw idempotency keys/private R2 keys/secrets. |
| Data lifecycle requests/items | D1 request evidence. | Legal/support retention to be defined. | Retain as compliance/support evidence. | Schema and admin APIs added. |
| Organizations | Phase 2-A adds additive organization metadata and creator ownership. | Retain while organization is active; legal/product must define closed-org retention. | Anonymize or retain according to future org lifecycle policy. | Schema and minimal APIs added; no lifecycle export/delete integration yet. |
| Organization memberships | Phase 2-A adds basic `owner`/`admin`/`member`/`viewer` memberships. | Retain while membership or organization is active; membership history policy is open. | Remove/anonymize according to future membership lifecycle policy. | Schema and minimal APIs added; no lifecycle export/delete integration yet. |
| Plans/entitlements | Phase 2-B adds plan and entitlement metadata with a seeded free plan. | Retain while referenced by subscriptions, usage, or ledger history. | Retain as product/billing configuration. | Foundation schema/helper only; no live payment provider. |
| Organization subscriptions/billing customers | Phase 2-B adds organization-scoped subscription and future provider mapping tables. | Retain according to billing/legal policy once payments exist. | Anonymize or retain according to billing legal requirements. | Placeholder only; no real payment provider data should be stored yet. |
| Credit ledger/usage events | Phase 2-B adds idempotent credit grants, consumption, and usage events. Phase 2-C writes org-scoped image generation debits when `/api/ai/generate-image` includes organization context. Phase 2-H writes org-scoped text generation debits for `/api/ai/generate-text`. Phase 2-M writes `admin_ai_image_test` debits only for platform-admin Admin AI image tests using supported charged models. Phase 4.3 adds bounded safe budget-policy metadata for the charged Admin BFL image-test branch. Phase 4.14 preserves the charged branch and does not create credit ledger rows for FLUX.2 Dev explicit-unmetered or blocked unsupported Admin Image models. | Retain for billing/cost/audit windows to be defined. | Retain/anonymize according to billing and abuse-review policy. | Helper/API foundation exists; lifecycle export/delete integration remains future work. Raw prompts, request bodies, provider payloads, auth headers, cookies, Stripe data, Cloudflare tokens, private R2 keys, and secrets should not be stored in usage metadata. |
| Member credit ledger/usage events | Migrations `0041` and `0042` add member-level credit grants, charges, usage events, and live credit-pack checkout tracking. | Retain for billing/cost/audit windows to be defined. | Retain/anonymize according to billing and abuse-review policy. | Lifecycle export/delete integration remains future work. These rows do not prove full live billing readiness. |
| AI usage attempts/reservations | Phase 2-D adds `ai_usage_attempts` for org-scoped image-generation idempotency, provider execution suppression, temporary result replay, and credit reservation/finalization state. Phase 2-E adds bounded scheduled/admin cleanup and admin-only sanitized inspection. Phase 2-F adds strict cleanup for expired temporary replay objects. Phase 2-H reuses the same table for org-scoped text generation and stores bounded generated-text replay metadata. Phase 2-M reuses it for charged BFL Admin AI image tests to prevent duplicate provider execution/debits and release reservations on provider failure. Phase 4.3 adds safe budget-policy metadata for those charged Admin BFL image-test attempts. Phase 4.14 does not add attempts for FLUX.2 Dev explicit-unmetered or blocked unsupported Admin Image models. | Short-to-medium operational and billing-dispute window to be defined; expired/stuck attempts should be cleaned through bounded batches. Temporary image replay objects should live only through the replay window and then be removed by prefix-scoped cleanup. Text replay metadata should live only through the attempt replay window and then be expired by metadata cleanup. | Retain/anonymize according to billing/audit policy; do not export raw hashes, raw budget fingerprints, or replay text by default. Cleanup releases stale reservations, expires replay metadata, and deletes only expired attempt-linked temporary image replay objects under `tmp/ai-generated/{userId}/{tempId}`. It does not delete attempt rows, ledger rows, usage-event rows, saved media, private media, audit/export archives, video output/poster objects, derivatives, or text assets. | Stores status, request fingerprint/idempotency hashes, sanitized temporary save-reference metadata, bounded text replay metadata for text attempts, Phase 2-M pricing summary metadata for admin image tests, Phase 4.3 budget-policy metadata, and safe error codes only; no raw prompts, image bytes, provider secrets, service-auth data, auth headers, cookies, Stripe data, Cloudflare tokens, private R2 keys, or raw request bodies. Admin responses omit raw hashes/fingerprints/temp keys/save references/replay text and expose sanitized counts only. Unsafe or unrelated object keys are skipped. |
| Member AI usage attempts/reservations | Phase 3.4 adds `member_ai_usage_attempts` for member personal image-generation idempotency, provider execution suppression, temporary result replay metadata, and member-credit reservation/finalization state. Phase 3.6 reuses the same member attempt foundation for member music parent reservations covering lyrics/audio/cover provider-cost work as one bundled charge. Phase 3.7 adds safe replay-unavailable, music cover-status, finalization, and scheduled cleanup behavior for member image/music attempts. Phase 3.8 reuses the foundation for member video parent reservations covering PixVerse/HappyHorse provider work, durable output ingest, saved-asset replay metadata, and exactly-once debit after persistence. | Short-to-medium operational and billing-dispute window to be defined; expired/stuck member attempts are released through bounded scheduled cleanup. Temporary image replay objects and safe persisted-asset music/video replay metadata should live only through the replay window; missing/expired replay must not re-run providers or double debit. | Retain/anonymize according to billing/audit policy; do not export raw hashes, fingerprints, prompts, lyrics, or internal R2 keys by default. Cleanup must not delete member ledger rows, member usage rows, saved media, private media, audit/export archives, video outputs, derivatives, or unrelated R2 objects. | Stores scoped idempotency keys, request fingerprints, status, safe result pointers, bounded result metadata, replay-unavailable state, cover `pending`/`succeeded`/`failed`/`skipped` status, and safe error codes only; no raw prompts, raw lyrics, cookies, auth tokens, provider secrets, service-auth data, raw request bodies, image/audio/video bytes, internal R2 keys, card data, or payment method details. |
| Admin AI text/embeddings/music/compare/live-agent idempotency attempts | Migration `0051` adds `admin_ai_usage_attempts` for Phase 4.8.1 admin text/embeddings duplicate suppression and conflict detection. Phase 4.8.2 adds admin-only sanitized list/detail inspection plus bounded scheduled/admin cleanup. Phase 4.9 reuses the same table for Admin Music metadata-only duplicate suppression/conflict detection. Phase 4.10 reuses it for Admin Compare metadata-only duplicate provider-fanout suppression/conflict detection. Phase 4.12 reuses it for Admin Live-Agent metadata-only stream-session duplicate suppression/conflict detection and observable stream completion/failure summaries. | Short-to-medium admin cost/audit window to be defined; expired pending/running rows are marked expired through bounded cleanup, while completed/succeeded/failed rows are retained by default. | Retain/anonymize according to admin AI audit policy; do not export raw prompts, raw lyrics, raw embedding input, raw compare prompts, raw Live-Agent messages/output, raw provider request bodies, generated text, embedding vectors, audio, compare outputs, provider response bodies, hashes, fingerprints, or internal metadata by default. Cleanup is non-destructive and does not delete rows, mutate credits, mutate billing ledgers, call providers, or purge R2 objects. | Stores admin user id, operation key, route, hashed idempotency key, request fingerprint, provider/model, budget scope, sanitized budget/caller-policy JSON, status, safe result metadata, bounded errors, and timestamps only; no raw prompts, raw lyrics, raw embedding input, raw compare prompts, raw Live-Agent messages/output, raw provider request bodies, generated text, embedding vectors, audio, compare outputs, provider response bodies, cookies, auth headers, Stripe data, Cloudflare tokens, secrets, private keys, or private R2 keys. Inspection responses omit raw idempotency keys/hashes and request fingerprints. |
| Admin runtime budget switches | Migration `0052` adds `admin_runtime_budget_switches` and `admin_runtime_budget_switch_events` for Phase 4.15.1 app-level Admin AI Budget Switch Control Plane state and update history. | Retain as admin/platform AI operational-control audit evidence until a retention policy defines pruning. | Retain/anonymize according to admin AI operations policy; do not export Cloudflare variable values, Cloudflare API tokens, secrets, raw prompts, provider payloads, or raw request bodies. | Stores allowlisted switch keys, booleans, bounded reasons, safe updater identifiers, event idempotency keys/request hashes, and timestamps only. Missing rows default disabled at runtime. The Admin UI cannot mutate Cloudflare master flags. |
| Platform budget cap limits/events | Migration `0053` adds `platform_budget_limits`, `platform_budget_limit_events`, and `platform_budget_usage_events` for Phase 4.17 `platform_admin_lab_budget` cap control and bounded usage evidence. | Retain as admin/platform AI operational cost-control evidence until a retention policy defines pruning. | Retain/anonymize according to admin AI operations policy; do not export raw prompts, raw embedding input, raw lyrics, raw compare prompts/results, raw Live-Agent messages/output, generated media, provider request/response bodies, Stripe data, Cloudflare variable values/tokens, secrets, raw idempotency keys, or raw request bodies. | Stores safe budget scope/window/operation/source/actor summaries, estimated units, daily/monthly window keys, source attempt/job ids, idempotency hashes, bounded reason/metadata, and timestamps only. It is not customer billing, does not change member/org billing, and covers only selected `platform_admin_lab_budget` operations. |
| Billing provider events/actions | Phase 2-I adds `billing_provider_events` and `billing_event_actions` for provider-neutral webhook/event ingestion. Phase 2-J/2-L reuse them for verified Stripe Testmode/live checkout events. Phase 2.1 reuses them for review-only live failure/refund/dispute/expired-checkout classification. Phase 2.2 stores bounded admin resolution metadata in the existing action summary for manual `resolved`/`dismissed` state. | Billing/legal retention window to be defined before broad live billing is enabled. | Retain as billing-event audit evidence; anonymize or purge only after legal/product policy. | Stores provider/event ids, payload hashes, sanitized summaries, verification/processing status, dry-run/deferred action summaries, and bounded operator resolution notes. Raw webhook bodies, signatures, card/bank/payment method data, provider secrets, invoices, and full provider payloads are not stored. Events can grant credits only through verified Testmode/live checkout handlers that pass server-side validation; Phase 2.2 review/resolution metadata does not grant, reverse, subtract, delete, or cancel credits/accounts automatically and does not call Stripe. |
| Billing checkout sessions | Phase 2-J adds `billing_checkout_sessions` for Stripe Testmode credit-pack Checkout Sessions. Phase 2-L extends it for live one-time credit packs with authorization/payment-state fields. | Billing/legal retention window to be defined before broad live billing is enabled. | Retain as billing reconciliation/audit evidence; anonymize or purge only after legal/product policy. | Stores Stripe Testmode/live session/payment intent/customer references, org/user ids, pack/amount/currency, provider mode, authorization scope, payment status, idempotency/request hashes, status, billing event linkage, and credit ledger linkage. It does not store card data, payment method data, raw Stripe payloads, raw webhook bodies, webhook signatures, or Stripe secrets. Checkout URLs are stored only for same-key retry and should be revisited before broader production billing. |
| Member subscriptions and credit buckets | Migration `0047_add_member_subscriptions_and_credit_buckets.sql` adds member subscription rows, subscription checkout-session rows, member credit buckets, and bucket event history. | Billing/legal retention window to be defined before broad live subscriptions are enabled. | Retain as billing reconciliation/audit evidence; anonymize or purge only after legal/product policy. | Scaffolding exists and Phase 2.2 exposes selected failure/refund/dispute/expired-checkout events for operator review/resolution metadata only, but automated remediation and approved reconciliation workflows still block live billing readiness claims. |
| Asset storage quota | Migration `0046_add_asset_storage_quota.sql` adds per-user asset storage usage tracking. | Retain while assets/account are active; recompute/anonymize policy must be defined. | Recompute from source assets where possible or anonymize with account deletion policy. | Quota rows must not replace source asset ownership checks or lifecycle deletion planning. |
| News Pulse / OpenClaw cache state | Migrations `0043`-`0045` add News Pulse cache/visual metadata and OpenClaw ingest nonce replay protection. Migration `0050` adds nullable visual budget metadata columns used by Phase 4.6 `openclaw_news_pulse_budget` controls. Phase 4.15 enforces `ENABLE_NEWS_PULSE_VISUAL_BUDGET` before visual provider generation/backfill. | Product/news cache and short security replay windows to be defined. | Purge/expire by reviewed content/security policy. Disabled Phase 4.15 visual attempts may record safe skipped status but must not mark visuals ready or delete existing visuals. | Visual budget metadata must remain bounded and sanitized: operation id, budget scope, kill-switch target `ENABLE_NEWS_PULSE_VISUAL_BUDGET`, provider/model class, item/content hash, attempt count, and safe runtime status only. It must not include secrets, raw provider request bodies, auth headers, cookies, Stripe data, Cloudflare tokens, private R2 keys, raw source payloads beyond public-safe article text, or raw provider credentials. |
| Pricing page / credit-pack / BITBI Pro UX | The pricing/account billing surfaces render free, one-time pack, and BITBI Pro subscription copy where enabled. | No separate page data retention. Underlying checkout/session/subscription/ledger/bucket/event records follow billing retention policy. | Remove or hide the page by static rollback if product rollout changes; do not delete checkout/ledger/subscription/bucket/event history through UI rollback. | Public production billing, invoices, customer portal, Stripe Tax, coupons, Connect, refunds/chargeback automation, reconciliation operations, and full live billing readiness remain unproven. Product/legal must approve public pricing and live billing terms before broader rollout. |
| Credits dashboard / live credit-pack UX | Phase 2-L adds `/account/credits.html` for platform admins and active organization owners to inspect balances and buy fixed live one-time credit packs. Phase 2-M updates the live catalog to 5,000 credits / 9.99 EUR and 12,000 credits / 19.99 EUR, and recent ledger activity may show sanitized `admin_ai_image_test` debits. Later account billing work adds member-level credit and subscription state to the same account billing surface. | No separate page data retention. Underlying checkout/session/ledger/bucket/subscription/event records follow billing retention policy. | Remove or hide the page by static rollback if rollout changes; do not delete checkout/ledger/bucket/subscription/event history through UI rollback. | This is a narrow gated live checkout canary surface only. Public production billing, invoices, customer portal, Stripe Tax, coupons, Connect, refunds/chargeback automation, reconciliation operations, and full production billing readiness remain unproven. |
| Admin Control Plane inspection surfaces | Frontend-only admin UI renders sanitized summaries from existing admin APIs and static readiness copy. | No separate data retention. Underlying API records follow their source-table policies. | Revert frontend files if the operator surface is removed; do not delete source operational/security/billing/lifecycle records as part of UI rollback. | Does not prove production readiness, does not expose secrets, and does not allow Cloudflare dashboard/secret editing. |
| Temporary R2 objects/cleanup queue | Cleanup/retry queue exists for some objects. | Short operational retry window. | Retry/delete stale objects. | Existing cleanup plus future lifecycle executor. |

## Current Enforcement

Phase 1-H enforces request creation, planning, approval state, idempotency, route policy registration, admin-only access, fail-closed rate limiting, same-origin checks, and byte-limited bodies. Phase 1-I adds bounded export archive generation and authorized admin archive download for approved export plans. Phase 1-J adds bounded expired-archive R2 cleanup and a safe executor pilot for reversible auth-state cleanup. Phase 2-A adds organization/membership storage. Phase 2-B adds billing/entitlement/credit-ledger storage and admin credit-grant APIs. Phase 2-C adds opt-in org-scoped image generation credit debits. Phase 2-D adds opt-in org-scoped image-generation usage attempts/reservations. Phase 2-E adds bounded cleanup for expired/stuck usage attempts, releases stale reservations without debits, expires replay metadata after its replay window, and adds sanitized admin inspection. Phase 2-F deletes only expired attempt-linked temporary replay objects after strict prefix/user/attempt validation. Phase 2-H adds backend-only org-scoped text generation credit debits and bounded text replay metadata. Phase 2-I adds provider-neutral billing event ingestion with raw-body synthetic verification, idempotent event storage, dry-run action planning, and sanitized admin inspection. Phase 2-J adds Stripe Testmode credit-pack checkout session tracking and verified Testmode checkout credit grants. Phase 2-K locks Testmode checkout to platform admins. Phase 2-L adds narrow live one-time credit-pack checkout and a gated Credits dashboard, backed by additive checkout authorization/payment-state metadata in migration `0040`. Phase 2-M adds no new storage; it reuses existing billing and attempt tables for platform-admin-only BFL Admin AI image-test charges. Later migrations add member credit ledgers/live checkout (`0041`/`0042`), News Pulse/OpenClaw storage (`0043`-`0045`), asset storage quota rows (`0046`), member subscription/credit bucket rows (`0047`), member personal AI usage attempts (`0048`) now used for member image, member music, and member video gateway attempts plus Phase 3.7 replay-unavailable, cover-status, finalization, and cleanup metadata, admin video job budget metadata columns (`0049`) used only by Phase 4.5 admin async video jobs, News Pulse visual budget metadata columns (`0050`) used only by Phase 4.6 OpenClaw/News Pulse visuals, and admin AI usage attempts (`0051`) used by Phase 4.8.1 admin text/embeddings idempotency plus Phase 4.8.2 cleanup/inspection plus Phase 4.9 Admin Music, Phase 4.10 Admin Compare, and Phase 4.12 Admin Live-Agent metadata-only idempotency. Phase 4.1 adds admin/platform AI budget policy design, registry metadata, and baseline/check metadata only. Phase 4.2 adds pure admin/platform budget policy helper contracts, deterministic tests, and stricter baseline/check metadata only. Phase 4.3 adds bounded budget-policy metadata to existing charged Admin BFL image-test responses, usage events, and attempts; it adds no new table, migration, broad admin/platform route migration, provider call, public billing behavior, or Admin UI behavior. Phase 4.4 adds read-only budget evidence reporting only. Phase 4.5 adds sanitized admin async video job budget metadata and queue budget summaries only. Phase 4.6 adds sanitized News Pulse visual budget metadata/status controls only. Phase 4.7 adds no new storage or migration; it validates/sanitizes signed caller-policy metadata inside the internal AI Worker, strips the reserved policy key before provider payload handling, and updates evidence/reporting for covered and baseline-allowed internal routes. Phase 4.8 adds no new storage or migration; admin text and embeddings test routes require a valid `Idempotency-Key`, compute sanitized `platform_admin_lab_budget` budget metadata in-memory, return safe admin-only budget/caller summaries, and propagate signed `budget_metadata_only` caller-policy metadata that the AI Worker strips before provider payload handling. Phase 4.8.1 stores metadata-only admin text/embeddings idempotency rows and intentionally does not store generated text or embedding vectors. Phase 4.8.2 adds no new storage or migration; it exposes admin-only sanitized list/detail records and marks expired pending/running rows through bounded scheduled/admin cleanup without destructive deletion. Phase 4.9 adds no new storage or migration; it stores only sanitized Admin Music attempt metadata and intentionally does not store raw prompts, lyrics, audio, or provider bodies. Phase 4.10 adds no new storage or migration; it stores only sanitized Admin Compare attempt metadata and intentionally does not store raw prompts, compare outputs, provider request bodies, or provider response bodies. Phase 4.11 adds no new storage or migration; it audits Admin Live-Agent only. Phase 4.12 adds no new storage or migration; it stores only sanitized Admin Live-Agent stream-session attempt metadata and intentionally does not store raw messages, streamed output, provider request bodies, or provider response bodies. Phase 4.13 adds no new storage or migration; it retires sync video debug from normal provider-cost operations as disabled-by-default/emergency-only and keeps async admin video jobs as the supported budgeted admin video path. Phase 4.14 adds no new storage or migration; it classifies Admin Image branches, keeps the existing charged branch unchanged, emits in-memory safe metadata only for the FLUX.2 Dev explicit-unmetered exception, and blocks unclassified Admin Image models before AI Worker/provider execution. Phase 4.15 adds no new storage or migration; it enforces default-disabled Cloudflare master runtime budget switches for already budget-classified admin/platform provider-cost paths before provider/queue/credit/durable-attempt work and records safe switch state in evidence only. Phase 4.15.1 adds migration `0052` and stores D1 app-level runtime budget switch state/history only; Cloudflare master flags remain deployment-owned, no Cloudflare API tokens or variable values are stored, and missing rows default disabled. Phase 4.17 adds migration `0053` and stores only `platform_admin_lab_budget` cap limits, cap update events, and bounded usage events; it is not customer billing or member/org billing. Phase 4.6/4.7/4.8/4.8.1/4.8.2/4.9/4.10/4.11/4.12/4.13/4.14/4.15 do not call providers in tests, call Stripe, mutate credits beyond the existing charged image branch, change public billing, migrate Admin video beyond Phase 4.5/4.13, migrate OpenClaw-News Pulse beyond Phase 4.6, migrate platform-background routes outside News Pulse visuals, globally hard-fail unrelated internal AI Worker routes, change member image/music/video billing behavior, change org-scoped member route behavior, add customer billing, extend platform caps beyond `platform_admin_lab_budget`, or enable live billing. The Pricing/Credits/BITBI Pro/account AI surfaces add no separate page storage but depend on migrations through `0053` for the current auth Worker behavior; the Admin Control Plane renders sanitized APIs and stores only Phase 4.15.1 switch state plus Phase 4.17 cap controls in D1. Org, billing, member subscription, credit bucket, member AI usage attempt, admin AI usage attempt, admin video job budget metadata, storage quota, News Pulse, and OpenClaw data are not yet part of export/delete planning. The system still does not execute irreversible deletion or hard-delete user/media/audit records by default.

Phase 4.16 adds no D1 table, migration, R2 object, or runtime route behavior. It only documents future live platform budget cap tables and exposes read-only cap-readiness evidence. Phase 4.17 adds the first narrow subset of that design through migration `0053` for `platform_admin_lab_budget` only. Other future cap tables or scopes have no retention clock, export/delete behavior, or live enforcement effect until later additive migrations exist.

Phase 4.21 adds migration `0055` and stores `platform_budget_evidence_archives` metadata plus sanitized archive objects in `AUDIT_ARCHIVE` under `platform-budget-evidence/`. Archive creation is admin-approved and evidence-only: it applies no repair, mutates no usage/source/repair rows, calls no providers, calls no Stripe APIs, mutates no credits, and does not change member/org/customer billing. Cleanup is admin-triggered/bounded in this phase and refuses keys outside the approved prefix.

Phase 6.4 adds migration `0056` with nullable ownership metadata columns on `ai_folders` and `ai_images`. Phase 6.5 assigns those fields only for new personal folder/image writes. Phase 6.6 adds read-only diagnostics over synthetic/source evidence. Phase 6.7 exposes those diagnostics to admins through bounded JSON/Markdown evidence reports. Phase 6.8 documents how operators collect and record those reports. Phase 6.9 records that in-repo main evidence is pending and adds a local summarizer for future reviewed JSON exports. Phase 6.10 records the current pending/blocked decision because no real main export is present. These phases do not backfill old rows, assign organization ownership, enforce owner metadata in access checks, list/move/delete R2 objects, or alter retention, lifecycle, gallery, quota, or R2 cleanup behavior.

## Contact Processor Policy Gap

Contact form content is sent through Resend and is not stored in a repo-owned D1 table. Until a processor workflow or repo-owned contact-message store exists:

- Contact messages are treated as high-PII.
- Export/delete requests involving contact messages require manual processor/mailbox review.
- No repository API currently promises contact-message export or deletion.
- Product/legal must define the Resend/mailbox retention window before compliance claims are made.

## Historical R2 Ownership Policy

Phase 1-J cleanup is intentionally narrow. It only deletes `AUDIT_ARCHIVE` objects whose keys match the generated export archive prefix. Do not enable deletion for these categories until ownership is proven by D1 rows or a dry-run owner-map backfill:

- Audit/activity archive JSONL chunks outside `data-exports/`.
- Legacy `PRIVATE_MEDIA` keys not linked to `profiles`.
- Legacy `USER_IMAGES` keys not linked to `ai_images`, `ai_text_assets`, or `ai_video_jobs`.
- Any provider/transient object without an owner row.
- Stripe Testmode/live checkout, billing-event, credit-ledger, and usage-event records unless a legal/product-approved billing retention policy allows deletion or anonymization.

## Required Before Destructive Deletion

1. Legal/product approval of retention windows and deletion exceptions.
2. Staging verification that every R2 object key pattern is owner-scoped and complete.
3. A two-step approval/execution model with idempotency and audit events.
4. A recovery grace period or explicit no-recovery policy.
5. Dry-run comparison of planned D1/R2 actions against expected user data.
6. Tests proving cross-user data is not included and audit records are retained/anonymized correctly.
7. Runbook updates for accidental deletion, rollback limitations, and support communication.
