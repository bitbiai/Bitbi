# Current Implementation Handoff

Date: 2026-05-16

Purpose: concise restart point for future Codex sessions after Phase 0 through Phase 4.17 plus the late Phase 4.15.1 Admin AI Budget Switch Control Plane, Admin Control Plane, Pricing / Credit Purchase, pricing asset-reference fix, Stripe Testmode config-diagnostics/admin-checkout lockdown work, the gated live Credits/Organization dashboards, member credit buckets, BITBI Pro subscription scaffolding, the 2026-05-15 Alpha Audit documentation reconciliation, Stripe live billing lifecycle operator-review queue/resolution UI foundations, read-only local billing reconciliation reporting, main-only release gate/checklist evidence planning, and AI Cost Gateway design/inventory/contract/operation-registry/member-image-pilot/member-music-gateway/member-video-gateway/hardening/enforcement-guard/admin-platform-budget-policy design/helper/charged-image-test hardening/read-only budget evidence/admin async video job budget enforcement/OpenClaw-News Pulse visual budget control/internal caller-policy guard/Admin text and embeddings budget enforcement/durable idempotency/cleanup-inspection/Admin Music budget enforcement/Admin Compare budget enforcement/Admin Live-Agent audit-design prep/Admin Live-Agent budget enforcement/sync video debug retirement decision/Admin Image branch handling/runtime budget kill-switch enforcement/D1 Admin UI budget switch control/live platform budget cap design-evidence work/first narrow `platform_admin_lab_budget` cap foundation. This is not a production deploy approval, live billing readiness claim, full platform budget cap rollout claim, or full AI cost readiness claim.

## Current Baseline

| Area | Current state |
| --- | --- |
| Branch | `main` |
| Latest observed commit before Phase 2-O work | `9198621 org` |
| Working tree at Phase 2-O start | Clean |
| Latest implemented work | Phase 4.17 first narrow `platform_admin_lab_budget` cap foundation on top of Phase 4.16 live platform budget cap design/evidence, Phase 4.15.1 Admin AI Budget Switch Control Plane, Phase 4.15 runtime budget kill-switch enforcement, Phase 4.14 Admin Image branch handling, Phase 4.13 sync video debug retirement decision, Phase 4.12 Admin Live-Agent budget enforcement, Phase 4.11 Admin Live-Agent budget flow audit/design prep, Phase 4.10 Admin Compare budget enforcement, Phase 4.9 Admin Music budget enforcement, Phase 4.8.2 Admin text/embeddings usage-attempt cleanup and inspection, Phase 4.8.1 Admin text/embeddings durable idempotency foundation, Phase 4.8 Admin text/embeddings budget enforcement, Phase 4.7 internal AI Worker caller-policy guard, Phase 4.6 OpenClaw/News Pulse visual budget controls, Phase 4.5 Admin async video job budget enforcement, Phase 4.4 read-only Admin/Platform AI budget evidence collector, Phase 4.3 charged Admin BFL image-test budget enforcement hardening, Phase 4.2 admin/platform AI budget policy helper contract/tests, Phase 4.1 admin/platform AI budget policy design, Phase 3.9 AI Cost Gateway enforcement guard and known-gap baseline, Phase 3.8 member video AI Cost Gateway migration, Phase 3.7 member image/music AI Cost Gateway replay-result/finalization/cleanup hardening, Phase 3.6 member music AI Cost Gateway migration, Phase 3.5 member music cost decomposition and gateway prep, Phase 3.4.1 main-only deploy/evidence checklist guidance for the Phase 3.4 member personal image AI Cost Gateway pilot, Phase 3.3 AI Cost Operation Registry/report-only baseline, Phase 3.2 AI Cost Gateway contract/helpers/tests, Phase 3.1 route inventory/roadmap/report-only local policy-gap check, Phase 2.6 main-only direct-release runbook/checklist and local readiness gate, Phase 2.4 read-only admin billing reconciliation report, Phase 2.3 Admin Control Plane UI for the Stripe live billing lifecycle review queue, prior Phase 2.2 review APIs, BITBI Pro member subscription/credit bucket foundation, and Alpha Audit documentation reconciliation |
| Latest corrective fix | Current-state documentation is reconciled to the release contract latest auth migration, `0053_add_platform_budget_caps.sql`; historical phase reports remain preserved as historical evidence. |
| Latest auth D1 migration | `0053_add_platform_budget_caps.sql` |
| Latest AI Worker Durable Object migration | `v1-service-auth-replay` |
| Baseline preflight | `npm run release:preflight` passed before Phase 2-M edits |
| Prior documentation reconciliation validation | `git diff --check`, release/config/route/body/data/activity/operational checks, Worker tests 346/346, static tests 163/163, asset-version checks, static build, and `npm run release:preflight` passed before Phase 2-L |
| Phase 2-K targeted validation | Baseline `npm run release:preflight` passed before edits; targeted `npm run check:js` and Phase 2-J/2-K-adjacent Worker tests passed during implementation |
| Phase 2-L targeted validation | Baseline `npm run release:preflight`, `npm run check:js`, `npm run check:route-policies`, `npm run test:workers` 352/352, and focused Credits static tests passed during implementation |
| Phase 2-M validation | Baseline `npm run release:preflight`, focused Phase 2-M/Phase 2-L Worker tests, focused Admin AI/Credits static tests, `npm run test:workers` 359/359, `npm run test:static` 168/168, `npm run build:static`, and `npm run release:preflight` passed during implementation |
| Phase 2-N validation | See `PHASE2N_ORGANIZATION_CONTEXT_AND_CREDIT_DEBIT_VISIBILITY_REPORT.md` and final Codex response for command results |
| Phase 2-O validation | See `PHASE2O_PRICING_HERO_LIVE_PACKS_AND_PROFILE_NAV_REPORT.md` and final Codex response for command results |
| Merge readiness | Ready only after current Phase 4.17 validation passes; no commit made by Codex |
| Staging readiness | Requires migrations/config/resources and functional staging verification |
| Production readiness | Blocked |
| Live billing readiness | Guarded one-time credit-pack, BITBI Pro subscription, review-only failure/refund/dispute/expired-checkout classification, admin-only review queue/resolution metadata code, Admin Control Plane UI for that queue, and a read-only local reconciliation report exist, with Phase 2.6 main-only evidence guidance for direct `main` deploys, but full live billing remains blocked without live evidence, approved remediation workflow, and legal/product approval |

## What Is Implemented

- Phase 0/1 security, reliability, route-policy, operational readiness, audit search, data lifecycle, export archive, cleanup, and safe executor foundations.
- Phase 2-A organizations, memberships, and basic RBAC.
- Phase 2-B billing plans, entitlements, credit ledger, usage events, and admin credit grants.
- Phase 2-C/D/E/F org-scoped image credit enforcement, usage attempts, reservations, replay, cleanup, admin inspection, and replay-object cleanup.
- Phase 2-G text route inspection/no-op.
- Phase 2-H org-scoped backend-only member text generation with entitlement/credit enforcement and bounded text replay.
- Phase 2-I provider-neutral billing event ingestion and synthetic test webhook foundation.
- Phase 2-J Stripe Testmode credit-pack checkout foundation and verified Testmode webhook credit grants.
- Phase 2-K server-side platform-admin-only Stripe Testmode checkout lockdown with explicit operator kill switch.
- Phase 2-L narrow live Stripe one-time credit packs for platform admins and active organization owners, plus `/account/credits.html`.
- Phase 2-M updated live credit-pack economics and charges existing organization credits when platform admins run Admin AI image tests for Flux 1 schnell or Flux 2 klein 9B.
- Phase 2-N adds `/account/organization.html`, shared active organization selection, a sanitized organization dashboard API, and safe Admin AI Lab credit-debit diagnostics so platform admins can verify which organization owns credits and receives admin image-test debits.
- Admin Control Plane frontend for existing sanitized admin APIs.
- Admin-only Pricing page for Free, 5,000 credits, and 12,000 credits, with paid CTAs routed to the Credits dashboard instead of direct checkout creation.
- Stripe Testmode config diagnostics that identify missing variable names without exposing values.
- Member credit ledger/bucket foundation through migration `0047`, separating subscription, purchased, and legacy/bonus credits while preserving existing ledger compatibility.
- BITBI Pro subscription checkout/cancel/reactivate scaffolding and paid-invoice subscription credit top-up handling behind live Stripe configuration gates.
- Per-user asset storage quota now resolves free versus active BITBI Pro limits through the subscription state.
- Phase 2.1 records selected live Stripe failed-payment, refund, dispute, and expired-checkout webhook events as sanitized billing action review metadata only. Phase 2.2 exposes those records through admin-only review list/detail/resolution metadata APIs. Phase 2.3 adds Admin Control Plane UI for the queue, safe detail panel, blocked-event warnings, and note/confirmation-gated `resolved` / `dismissed` actions with generated `Idempotency-Key`. Phase 2.4 adds `GET /api/admin/billing/reconciliation` and an Admin Control Plane panel that summarizes local D1 billing events, checkout sessions, credit ledgers, subscriptions, and review states as read-only risk signals. Phase 2.6 adds a main-only direct-release runbook/checklist and `check:main-release-readiness` local gate because the owner deploys from `main`. These phases do not automatically grant, reverse, subtract, delete, cancel, refund, call Stripe, claw back credits, resolve credits/accounts beyond manual metadata, deploy, or prove production readiness.
- Phase 3.1 adds `docs/ai-cost-gateway/` route inventory/design/roadmap docs and report-only `check:ai-cost-policy` / `test:ai-cost-policy` tooling. It does not change runtime AI charging behavior, call AI providers, mutate billing, require idempotency, reserve credits, suppress duplicate provider calls, add provider replay/cache, or prove production readiness.
- Phase 3.2 adds AI Cost Gateway helpers in `workers/auth/src/lib/ai-cost-gateway.js` plus `test:ai-cost-gateway`. It normalizes operation configs, builds safe request fingerprints and scoped idempotency keys, and returns pure gateway plans. Phase 3.4 currently uses it only for member personal image generation.
- Phase 3.3 adds an AI Cost Operation Registry in `workers/auth/src/lib/ai-cost-operations.js` plus `test:ai-cost-operations`. It records normalized target operation configs, current enforcement metadata, route-policy baselines, and source-file inventory for known AI cost operations. Phase 3.9 upgrades `check:ai-cost-policy` from report-only into a default local guard that allows only explicit known-gap baseline items and fails on provider-cost drift or member image/music/video gateway regressions.
- Phase 3.4 migrates only member personal `/api/ai/generate-image` to the AI Cost Gateway pilot. That path now requires `Idempotency-Key`, builds a gateway fingerprint from the registry operation, records a member-credit reservation in `member_ai_usage_attempts`, suppresses same-key duplicate provider execution, replays safe stored temporary image metadata when available, debits exactly once after provider success, and releases/no-charges on provider failure. It does not migrate org-scoped image/text, music, video, admin AI, platform/background AI, internal AI Worker routes, pricing, Stripe, or public billing behavior.
- Phase 3.4.1 adds only main-only release/evidence guidance for the Phase 3.4 pilot. It adds `docs/production-readiness/PHASE3_MEMBER_IMAGE_GATEWAY_MAIN_CHECKLIST.md`, updates main-only evidence docs, and clarifies that remote migration `0048` must be applied and verified before auth Worker deployment. It does not deploy, run remote migrations, call providers, change route behavior, change credit behavior, enable live billing, or prove production readiness.
- Phase 3.5 decomposes member music generation for gateway migration prep. It adds `docs/ai-cost-gateway/MEMBER_MUSIC_COST_DECOMPOSITION.md`, refines registry entries for `member.music.generate`, `member.music.lyrics.generate`, `member.music.audio.generate`, and `member.music.cover.generate`, and strengthens report-only AI cost policy tests/check output.
- Phase 3.6 migrates only member `/api/ai/generate-music` to the AI Cost Gateway. It requires a valid `Idempotency-Key`, creates one parent `member_ai_usage_attempts` reservation before lyrics/audio/cover work, suppresses same-key duplicate in-progress/completed provider execution, rejects same-key/different-body conflicts, debits exactly once after required audio persistence, records safe replay metadata without raw prompts/lyrics/secrets/R2 keys, releases/no-charges on lyrics/audio provider failure, treats storage/billing finalization failure as terminal/no paid replay, and includes cover generation in the parent music bundle with no separate visible charge. It does not add a migration, call real providers in tests, call Stripe, deploy, change public pricing, migrate video/admin/platform/internal/OpenClaw routes, or prove production readiness.
- Phase 3.7 hardens only already migrated member image/music gateway flows. Same-key completed requests with missing/expired replay metadata do not re-run providers or double debit, member image/music replay responses omit raw prompt/lyrics, member music cover generation writes safe `pending`/`succeeded`/`failed`/`skipped` status back to the parent attempt, finalization failures are explicit terminal states, and scheduled cleanup releases expired member attempts and deletes only approved linked temp replay objects. It does not add a migration, call real providers in tests, call Stripe, deploy, change public pricing, migrate video/admin/platform/internal/OpenClaw routes, or prove production readiness.
- Phase 3.8 migrates only member `/api/ai/generate-video` to the AI Cost Gateway. It requires a valid `Idempotency-Key`, uses `member.video.generate`, reserves one parent `member_ai_usage_attempts` row before PixVerse/HappyHorse provider work and remote output ingest, suppresses same-key duplicate in-progress/completed provider execution, rejects same-key/different-body conflicts, debits exactly once only after durable video persistence, stores safe saved-asset replay metadata without raw prompt/internal R2 keys, returns replay-unavailable without re-running providers or double-debiting, and marks provider/storage/billing finalization failures as terminal no-charge/safe states. It does not add a migration, call real providers in tests, call Stripe, deploy, change public pricing, migrate admin video jobs/admin/platform/internal/OpenClaw routes, or prove production readiness.
- Phase 3.9 adds only AI Cost Gateway enforcement guardrails and a known-gap baseline. `config/ai-cost-policy-baseline.json` explicitly lists accepted-for-now admin/platform/background/OpenClaw/internal provider-cost gaps, and default `npm run check:ai-cost-policy` now fails on unregistered provider-cost source files, unbaselined route-policy gaps, duplicate registry/baseline ids, missing baseline references, and member image/music/video gateway regressions. It does not change runtime route behavior, credit debit behavior, provider routing, migrations, pricing, admin/platform/internal route behavior, or production/live billing readiness.
- Phase 4.1 adds only admin/platform AI budget policy design and local guard metadata. `docs/ai-cost-gateway/ADMIN_PLATFORM_BUDGET_POLICY.md` defines budget scopes; `workers/auth/src/lib/ai-cost-operations.js` records target budget policy metadata; `config/ai-cost-policy-baseline.json` records target budget scopes and temporary allowance reasons; `check:ai-cost-policy` groups remaining admin/platform/internal/OpenClaw gaps by budget scope. It does not change runtime route behavior, credit debit behavior, provider routing, migrations, pricing, admin/platform/internal route behavior, Admin UI, or production/live billing readiness.
- Phase 4.2 adds only pure admin/platform AI budget policy contracts and deterministic tests. `workers/auth/src/lib/admin-platform-budget-policy.js` defines budget-scope validation, kill-switch metadata, safe audit fields, deterministic fingerprints, and plan classification for future admin/platform migrations; `test:admin-platform-budget-policy` covers the helper; and the baseline/check now requires kill-switch targets or explicit exemptions plus future enforcement paths for every accepted gap. The helper is not imported by runtime routes and does not change provider calls, credit debits, admin/platform/internal route behavior, Admin UI, or production/live billing readiness.
- Phase 4.3 imports the Phase 4.2 helper only into the existing charged Admin BFL image-test branch. Charged platform-admin image tests now carry safe `admin_org_credit_account` budget-policy plan/audit metadata in the response, usage event, and attempt metadata, including operation id, provider family/model, estimated credits, idempotency policy, and kill-switch target. It preserves the existing selected-organization charge boundary, no-charge-on-provider-failure behavior, exactly-once debit semantics, and uncharged/baselined status for broad admin AI routes. Phase 4.15 enforces the charged model-specific switch before provider work or credit debit.
- Phase 4.4 adds only read-only Admin/Platform AI budget evidence reporting. `workers/auth/src/lib/admin-platform-budget-evidence.js`, `GET /api/admin/ai/budget-evidence`, and `npm run report:ai-budget-evidence` summarize registry, baseline, route-policy, and Phase 4.3 metadata coverage. It does not migrate routes, enforce runtime budgets, call providers, call Stripe, mutate billing/credits/D1/R2, deploy, or prove production/live billing readiness.
- Phase 4.5 covers only admin async video job budget metadata/enforcement. `workers/auth/src/lib/ai-video-jobs.js` uses the Phase 4.2 admin/platform budget helper for `POST /api/admin/ai/video-jobs`, stores sanitized `platform_admin_lab_budget` metadata in `ai_video_jobs` via additive migration `0049_add_admin_video_job_budget_metadata.sql`, includes a bounded queue budget summary, validates job budget state before internal video task create/poll, and suppresses duplicate provider task creation after a create attempt. It preserves existing admin-only access, production MFA policy, required `Idempotency-Key`, output/poster behavior, service-auth semantics, and no-credit-debit behavior. Phase 4.15 enforces `ENABLE_ADMIN_AI_VIDEO_JOB_BUDGET` before job creation/queueing; live caps and broad Admin AI/internal route migrations remain future work.
- Phase 4.6 covers only OpenClaw/News Pulse visual budget metadata/status controls. `workers/auth/src/lib/news-pulse-visuals.js` uses the Phase 4.2 admin/platform budget helper for ingest-triggered and scheduled News Pulse visual generation, stores sanitized `openclaw_news_pulse_budget` metadata in `news_pulse_items` via additive migration `0050_add_news_pulse_visual_budget_metadata.sql`, blocks invalid budget policy before provider execution, and preserves ready/pending/status/attempt duplicate provider-call suppression. It preserves signed OpenClaw ingest, public News Pulse read behavior, Admin video behavior from Phase 4.5, member/org route behavior, service-auth semantics, and no-credit-debit behavior. Phase 4.15 enforces `ENABLE_NEWS_PULSE_VISUAL_BUDGET` before provider visual generation/backfill while public reads remain unaffected.
- Phase 4.7 covers only internal AI Worker caller-policy validation/metadata handling. `workers/shared/ai-caller-policy.mjs` defines the signed reserved JSON body key `__bitbi_ai_caller_policy`; `workers/ai/src/lib/caller-policy.js`, `workers/ai/src/index.js`, and `workers/ai/src/lib/validate.js` validate policy after service-auth and strip it before provider handlers; auth Worker callers propagate safe metadata for covered caller paths where feasible. It adds no migration, deploy, Stripe call, real provider call in tests, credit mutation, public billing change, broad Admin AI migration, Admin music/text/compare/live-agent migration, OpenClaw/News Pulse change beyond Phase 4.6 compatibility, global hard-fail internal route migration, member image/music/video billing behavior change, or production/live billing readiness.
- Phase 4.8 covers only Admin text and embeddings budget enforcement. `workers/auth/src/routes/admin-ai.js` uses the Phase 4.2 admin/platform budget helper for `POST /api/admin/ai/test-text` and `POST /api/admin/ai/test-embeddings`, requires valid `Idempotency-Key`, returns sanitized `platform_admin_lab_budget` metadata, and propagates signed `budget_metadata_only` caller-policy metadata to the AI Worker. It adds no migration, durable replay store, deploy, Stripe call, real provider call in tests, credit mutation, credit clawback, public billing change, Admin music/video/compare/live-agent migration, OpenClaw/News Pulse change, platform/background migration, unrelated internal route migration, member image/music/video or org-scoped member route behavior change, or production/live billing readiness.
- Phase 4.8.1 covers only Admin text/embeddings durable idempotency. `workers/auth/src/lib/admin-ai-idempotency.js` and additive migration `0051_add_admin_ai_usage_attempts.sql` add metadata-only attempt rows for `POST /api/admin/ai/test-text` and `POST /api/admin/ai/test-embeddings`; same-key/same-request pending, completed, or failed attempts do not re-run provider-cost work, and same-key/different-request retries conflict before provider execution. Completed duplicate replay is metadata-only with `result: null`; raw prompts, raw embedding input, generated text, embedding vectors, provider request bodies, cookies, auth headers, Stripe data, Cloudflare tokens, secrets, private keys, and private R2 keys are not stored. It does not migrate Admin music/video/compare/live-agent, Admin video beyond Phase 4.5, OpenClaw/News Pulse beyond Phase 4.6, platform/background AI, unrelated internal AI Worker routes, member image/music/video, org-scoped member image/text, public billing, Stripe, credit clawback, or live billing.
- Phase 4.8.2 covers only Admin text/embeddings usage-attempt operability. It adds admin-only sanitized list/detail endpoints, a dry-run-default bounded cleanup endpoint, and scheduled bounded cleanup for `admin_ai_usage_attempts`. Cleanup marks only expired pending/running rows, retains completed/failed rows, does not delete rows or R2 objects, and does not call providers, call Stripe, mutate credits, mutate billing, migrate Admin Music/Compare/Live-Agent, change member/org behavior, or enable live billing.
- Phase 4.9 covers only Admin Music budget enforcement. `POST /api/admin/ai/test-music` now requires a valid `Idempotency-Key`, builds sanitized `platform_admin_lab_budget` metadata with kill-switch target `ENABLE_ADMIN_AI_MUSIC_BUDGET`, stores metadata-only `admin_ai_usage_attempts` rows before the internal provider call, suppresses duplicate same-key provider execution, conflicts same-key/different-request retries, propagates signed caller-policy metadata to `/internal/ai/test-music`, and keeps replay metadata-only with `result: null`. Phase 4.15 enforces the switch before attempts/provider work. It adds no migration, does not store raw prompts, lyrics, audio, or provider bodies, does not debit credits, and does not migrate Admin Live-Agent, sync video debug, unmetered admin image, member routes, org-scoped member routes, OpenClaw/News Pulse beyond Phase 4.6, or broad internal routes.
- Phase 4.10 covers only Admin Compare budget enforcement. `POST /api/admin/ai/compare` now requires a valid `Idempotency-Key`, builds sanitized `platform_admin_lab_budget` metadata with kill-switch target `ENABLE_ADMIN_AI_COMPARE_BUDGET`, stores metadata-only `admin_ai_usage_attempts` rows before internal multi-model compare fanout, suppresses duplicate same-key provider fanout, conflicts same-key/different-request retries, propagates signed caller-policy metadata to `/internal/ai/compare`, and keeps completed duplicate replay metadata-only with `result: null`. Phase 4.15 enforces the switch before attempts/provider fanout. It adds no migration, does not store raw prompts, compare outputs, provider request bodies, or provider response bodies, does not debit credits, and does not migrate Admin Live-Agent, sync video debug, unmetered admin image, member routes, org-scoped member routes, OpenClaw/News Pulse beyond Phase 4.6, Admin Music beyond Phase 4.9, or broad internal routes.
- Phase 4.11 covers only Admin Live-Agent budget flow audit/design prep. It adds `docs/ai-cost-gateway/ADMIN_LIVE_AGENT_BUDGET_FLOW_AUDIT.md`, records Phase 4.12 target scope `platform_admin_lab_budget`, kill-switch target `ENABLE_ADMIN_AI_LIVE_AGENT_BUDGET`, metadata-only replay policy, request-level idempotency target, durable parent stream-session attempt target, caller-policy propagation target, and stream/failure finalization test plan. It adds no migration and does not change runtime behavior. Phase 4.15 later enforces the runtime switch for the implemented Live-Agent path.
- Phase 4.12 covers only Admin Live-Agent budget enforcement. `POST /api/admin/ai/live-agent` now requires a valid `Idempotency-Key`, builds sanitized `platform_admin_lab_budget` metadata with kill-switch target `ENABLE_ADMIN_AI_LIVE_AGENT_BUDGET`, stores metadata-only stream-session `admin_ai_usage_attempts` rows before the internal provider stream, suppresses duplicate same-key provider streams, conflicts same-key/different-request retries, propagates signed caller-policy metadata to `/internal/ai/live-agent`, and tracks observable stream completion/failure without storing raw messages, streamed output, or provider bodies. Phase 4.15 enforces the switch before stream attempts/provider setup. It adds no migration, does not debit credits, does not add full stream replay, and does not migrate sync video debug, unmetered admin image, member routes, org-scoped routes, OpenClaw/News Pulse beyond Phase 4.6, Admin Text/Embeddings beyond Phase 4.8/4.8.1/4.8.2, Admin Music beyond Phase 4.9, Admin Compare beyond Phase 4.10, Admin Video beyond Phase 4.5, or broad internal routes.
- Phase 4.13 audits and retires only the synchronous Admin Video Debug path. `POST /api/admin/ai/test-video` remains disabled by default and returns before body parsing, rate limiting, AI Worker/service binding calls, provider calls, queue work, credit mutation, or billing mutation unless `ALLOW_SYNC_VIDEO_DEBUG=true` is explicitly approved for emergency compatibility. Async admin video jobs remain the supported budgeted admin video path. Phase 4.13 adds no migration and does not change Admin async video job behavior, Admin Text/Embeddings/Music/Compare/Live-Agent behavior, unmetered Admin Image behavior, OpenClaw/News Pulse behavior, member image/music/video behavior, org-scoped member behavior, public pricing, Stripe, credits, or live billing readiness.
- Phase 4.14 handles only Admin Image branch classification. The charged priced image-test subset remains on the selected-organization `admin_org_credit_account` path with required organization context, required `Idempotency-Key`, org credit check/debit, no-charge-on-provider-failure, exactly-once debit, and safe budget/caller metadata. FLUX.2 Dev remains an intentional `explicit_unmetered_admin` admin-only exception with safe metadata and kill-switch target `ENABLE_ADMIN_AI_UNMETERED_IMAGE_TESTS`; it does not debit credits or add durable idempotency/replay. Unknown or unclassified Admin Image models block before AI Worker/provider execution. Phase 4.15 enforces model-specific charged image switches and `ENABLE_ADMIN_AI_UNMETERED_IMAGE_TESTS` before provider work.
- Phase 4.15 covers only runtime budget kill-switch enforcement for already budget-classified admin/platform provider-cost paths. The new helper treats missing/empty/false/unrecognized flag values as disabled and accepts `1`, `true`, `yes`, and `on` as enabled. Disabled covered admin routes return `admin_ai_budget_disabled` before provider/proxy/queue/credit/durable-attempt work; disabled News Pulse visual generation records a safe skipped status before provider execution while public reads remain unaffected. It adds no migration, no live platform caps, no new route migration, no member/org billing behavior change, no public billing change, no Stripe call, no real provider call in tests, and no live billing readiness.
- Phase 4.15.1 adds the D1-backed Admin AI Budget Switch Control Plane on top of the Phase 4.15 Cloudflare master switches. Additive migration `0052_add_admin_runtime_budget_switches.sql` creates `admin_runtime_budget_switches` and `admin_runtime_budget_switch_events`; `GET /api/admin/ai/budget-switches` lists safe master/app/effective state, `PATCH /api/admin/ai/budget-switches/:switchKey` writes only D1 app-level state with an operator reason and `Idempotency-Key`, and the Admin Control Plane shows an AI Budget Switches panel. Effective provider-cost execution now requires both the Cloudflare master flag and the D1 app switch. Missing D1 rows, disabled app switches, unavailable D1, or disabled/missing Cloudflare flags fail closed before provider/proxy/queue/credit/durable-attempt work. The UI cannot mutate Cloudflare variables, does not store Cloudflare API tokens, and does not change Phase 4.16 live cap status.
- Phase 4.16 is design/evidence/checking only for live platform budget caps. It adds `docs/ai-cost-gateway/LIVE_PLATFORM_BUDGET_CAPS_DESIGN.md`, cap-readiness metadata in the operation registry/check output, and read-only evidence fields showing `liveBudgetCapsStatus: not_implemented`, switch-enforced versus cap-enforced distinction, budget-scope countability, estimated-cost-unit availability, durable completion timestamp availability, and recommended first scope `platform_admin_lab_budget`. It adds no migration, no runtime cap enforcement, no route behavior change, no provider calls, no credit mutation, and no live billing readiness.
- Phase 4.17 adds the first narrow live platform budget cap foundation only for `platform_admin_lab_budget`. Additive migration `0053_add_platform_budget_caps.sql` creates `platform_budget_limits`, `platform_budget_limit_events`, and `platform_budget_usage_events`; admin-only cap APIs and the Admin Control Plane cap panel can configure daily/monthly limits and inspect bounded usage summaries. Covered admin lab routes still require Cloudflare master switch plus D1 app switch before the cap check. Missing active caps, unavailable D1, or cap exhaustion fail closed before provider/internal AI/queue/durable-attempt work. Successful supported completions record bounded usage evidence after provider-cost success. This is not customer billing, Stripe billing, member/org billing, full cap coverage for other scopes, or production/live billing readiness.

## What Is Not Implemented

- Full production payment processing, customer portal, Stripe Tax, Stripe Connect, coupons, invoice operations, refund/chargeback automation, automated failed-payment remediation, approved billing remediation workflow, or live billing readiness evidence. Phase 2.4 is a read-only local reconciliation report only, not remediation. Phase 2.6 is release evidence/checklist tooling only, not runtime behavior or production approval.
- Full tenant-owned asset migration or full enterprise tenant isolation.
- A single runtime AI Cost Gateway for all expensive member/admin/platform AI routes with required idempotency, pre-provider reservations, provider-result replay/cache, live platform caps, and release-on-failure semantics. Phase 3.4 covers member personal image generation, Phase 3.6 covers member music generation, Phase 3.7 hardens those two migrated flows, and Phase 3.8 covers member video generation. Phase 4.3 hardens only the charged Admin BFL image-test branch with budget-policy metadata; Phase 4.4 reports evidence only; Phase 4.5 covers only admin async video jobs; Phase 4.6 covers only OpenClaw/News Pulse visuals; Phase 4.7 adds caller-policy validation/metadata stripping; Phase 4.8/4.8.1/4.8.2 cover only admin text/embeddings metadata-only budget enforcement plus durable idempotency/cleanup/inspection; Phase 4.9 covers only Admin Music metadata-only budget enforcement plus durable idempotency; Phase 4.10 covers only Admin Compare metadata-only budget enforcement plus durable idempotency; Phase 4.12 covers only Admin Live-Agent metadata-only stream-session budget enforcement; Phase 4.13 retires sync video debug as disabled-by-default/emergency-only rather than a normal provider-cost path; Phase 4.14 classifies Admin Image branches and blocks unclassified models before provider execution; Phase 4.15 enforces runtime budget switches only for already budget-classified admin/platform paths; Phase 4.16 designs live platform caps and reports cap status only. Platform/background AI outside News Pulse visuals, actual live platform cap enforcement, and remaining baseline-allowed internal AI Worker provider routes remain unmigrated.
- User self-service privacy center.
- Irreversible deletion/anonymization executor.
- Legal/compliance certification.
- Full Cloudflare IaC/drift enforcement, live alert evidence, load budgets, or restore drill evidence.

## Production Blockers

- Apply auth migrations through `0053`.
- Provision required auth/AI/contact secrets and verify without printing values.
- Verify matching `AI_SERVICE_AUTH_SECRET` in auth and AI Workers.
- Verify `SERVICE_AUTH_REPLAY` binding and DO migration.
- Verify auth D1/R2/Queue/service/DO bindings.
- Configure Stripe Testmode values: `STRIPE_MODE=test`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_CHECKOUT_SUCCESS_URL`, `STRIPE_CHECKOUT_CANCEL_URL`.
- Enable `ENABLE_ADMIN_STRIPE_TEST_CHECKOUT=true` only for a bounded admin-only Testmode canary window; leave it absent/false otherwise.
- Verify Stripe Testmode checkout and webhook behavior in staging.
- Configure live Stripe values only if running the Phase 2-L canary: `ENABLE_LIVE_STRIPE_CREDIT_PACKS`, `STRIPE_LIVE_SECRET_KEY`, `STRIPE_LIVE_WEBHOOK_SECRET`, `STRIPE_LIVE_CHECKOUT_SUCCESS_URL`, `STRIPE_LIVE_CHECKOUT_CANCEL_URL`.
- Keep `ENABLE_LIVE_STRIPE_CREDIT_PACKS=false` except during the explicitly bounded live credit-pack canary.
- Verify live checkout/webhook exactly-once credit grant behavior only through the operator-run canary.
- Configure and verify BITBI Pro subscription values only if running an explicitly approved subscription canary: `ENABLE_LIVE_STRIPE_SUBSCRIPTIONS`, `STRIPE_LIVE_SUBSCRIPTION_PRICE_ID`, `STRIPE_LIVE_SUBSCRIPTION_SUCCESS_URL`, and `STRIPE_LIVE_SUBSCRIPTION_CANCEL_URL`.
- Do not claim live billing readiness until Phase 2.3 failed-payment/refund/dispute/chargeback/expired-checkout review capture, Admin Control Plane UI, resolution metadata, Phase 2.4 read-only reconciliation reporting, and Phase 2.6 main-only evidence gates are verified live and billing remediation workflows are implemented or explicitly accepted with documented operational controls.
- Verify Phase 2-M/4.3 admin-only BFL image-test charging against real staging/canary org credit balances, including no provider call on missing org/idempotency/insufficient credits, no debit on provider failure, exactly-once debit on retry, safe budget-policy metadata, and ledger/dashboard visibility.
- Verify Phase 2-N organization selection in staging/canary, including solo-admin BITBI auto-selection, platform-admin organization selector, owner-only Organization page access, Organization/Credits/Admin AI Lab context sharing, and visible debit diagnostics after BFL admin image tests.
- Do not claim AI cost readiness until additional route migrations require idempotency, reserve credits/budget before provider calls, suppress duplicate provider execution, replay safe results or safe replay-unavailable responses, and release reservations on provider failure across provider-cost routes beyond the migrated member image/music/video flows, charged Admin BFL image tests, admin async video jobs, News Pulse visuals, admin text/embeddings durable metadata-only idempotency, Admin Music durable metadata-only idempotency, Admin Compare durable metadata-only idempotency, and Admin Live-Agent metadata-only stream-session idempotency. Sync video debug is retired/disabled-by-default, not production-ready budget enforcement.
- Verify Phase 4.15.1 Admin AI Budget Switch Control Plane evidence after remote migration `0052`: Cloudflare master flags remain hard gates, D1 app switches default disabled when missing, Admin UI cannot override disabled/missing master flags, and no Cloudflare API tokens are stored. Verify Phase 4.17 cap-foundation evidence after remote migration `0053`: `platform_admin_lab_budget` caps default fail closed without active daily/monthly limits, usage summaries are bounded/sanitized, and other budget scopes remain future work.
- Verify Admin Control Plane and Pricing in staging against real APIs.
- Run live health/security-header checks with explicit URLs.
- Verify dashboard-managed WAF/static headers/RUM/alerts or codify them.
- Execute and record a D1/R2 restore drill.

## Stripe / Testmode Status

- Checkout route: `POST /api/orgs/:id/billing/checkout/credit-pack`.
- Webhook route: `POST /api/billing/webhooks/stripe`.
- Checkout is server-side platform-admin-only and still requires active org owner/admin membership for the target organization.
- Checkout requires `ENABLE_ADMIN_STRIPE_TEST_CHECKOUT=true`, `STRIPE_MODE=test`, `STRIPE_SECRET_KEY`, `STRIPE_CHECKOUT_SUCCESS_URL`, and `STRIPE_CHECKOUT_CANCEL_URL`.
- Checkout does not require `STRIPE_WEBHOOK_SECRET`.
- Webhook requires `STRIPE_WEBHOOK_SECRET`.
- Live mode is rejected/disabled.
- Credits are granted only after verified Testmode `checkout.session.completed`, server-side pack/amount/currency/org/user/payment validation, and confirmation that the persisted checkout session was created by an active platform admin.
- Duplicate webhooks must not double-grant.

## Pricing / Admin Rollout Status

- Pricing route: `/pricing.html`.
- Header link visible only to authenticated admins.
- Direct pricing route access is frontend admin-gated.
- Exposed options: Free, 5,000 credits for 9.99 EUR, and 12,000 credits for 19.99 EUR.
- Free tier copy: 10 successful legacy image generations per UTC day.
- Pricing paid CTAs open `/account/credits.html`; checkout authorization remains enforced by the existing Credits dashboard/backend routes and flags.
- Runtime Pricing page copy no longer presents the page as a Testmode surface.
- No public rollout or unrestricted checkout activation.

## Credits Dashboard / Live Credit Pack Status

- Credits route: `/account/credits.html`.
- Profile navigation adds `Credits` below `Wallet` only for platform admins and active organization owners.
- Organization route: `/account/organization.html`.
- Profile navigation adds `Organization` near Wallet/Credits only for platform admins and active organization owners.
- Platform-admin profile navigation visibility is based on the authenticated account role, not organization-dashboard availability or a selected organization. This lets a platform admin reach Organization/Credits pages to diagnose organization context.
- Active frontend organization context is stored as localStorage key `bitbi.activeOrganizationId`; it is convenience-only and backend authorization remains authoritative.
- Backend live checkout route: `POST /api/orgs/:id/billing/checkout/live-credit-pack`.
- Backend Credits dashboard route: `GET /api/orgs/:id/billing/credits-dashboard`.
- Backend Organization dashboard route: `GET /api/orgs/:id/organization-dashboard`.
- Live webhook route: `POST /api/billing/webhooks/stripe/live`.
- Allowed buyers: platform/global admins and active owners of the target organization.
- Explicitly denied: unauthenticated users, normal users, org admins unless also platform admins, members, viewers, and owners of other organizations.
- Live packs: `live_credits_5000` = 5,000 credits for 9.99 EUR; `live_credits_12000` = 12,000 credits for 19.99 EUR. `live_credits_10000` is not offered for new live checkout; existing persisted legacy sessions can only be validated against their stored server-side row.
- Checkout requires `ENABLE_LIVE_STRIPE_CREDIT_PACKS=true`, live-like `STRIPE_LIVE_SECRET_KEY`, and configured live success/cancel URLs.
- Credits are granted only after verified live Stripe webhook completion and server-side revalidation of persisted checkout row, buyer authorization scope, pack, amount, currency, payment status, and organization/user.
- This is not public pricing, not subscriptions, not invoices, not customer portal, not Stripe Tax, and not full production billing readiness.

## Admin AI Image Test Credit Charging Status

- Route: `POST /api/admin/ai/test-image`.
- Scope: platform/global admin area only; no normal user, org owner, org admin, member, viewer, or public route access is added.
- Chargeable models in this phase: `@cf/black-forest-labs/flux-1-schnell`, `@cf/black-forest-labs/flux-2-klein-9b`, and proxied `black-forest-labs/flux-2-klein-9b`.
- Target organization: required through `organization_id` / `organizationId`; credits are deducted from the selected organization's existing credit balance.
- Admin AI Lab now uses the shared active organization context. If no organization is selected for a charged BFL image test, the UI/backend fail before provider execution with `Select an organization before running this charged image test.`
- Successful charged tests return safe diagnostics including organization id/name, model id, charged credits, ledger/usage/attempt ids when available, idempotency status, and balance before/after.
- Idempotency: charged BFL admin image tests require `Idempotency-Key`; same key/body is replay-safe without duplicate debit/provider call, and same key/different body conflicts.
- Default deductions: Flux 1 schnell default admin image test = 1 credit; Flux 2 klein 9B default text-only <=1MP admin image test = 10 credits.
- Provider failure: no debit; billing finalization failure does not return or persist an uncharged paid result.

## Docs To Read First Next Time

1. `SAAS_PROGRESS_AND_CURRENT_STATE_REPORT.md`
2. `AUDIT_NEXT_LEVEL.md`
3. `AUDIT_ACTION_PLAN.md`
4. `PHASE2O_PRICING_HERO_LIVE_PACKS_AND_PROFILE_NAV_REPORT.md`
5. `PHASE_PRICING_PAGE_CREDIT_PACKS_REPORT.md`
6. `PHASE2K_ADMIN_STRIPE_TESTMODE_LOCKDOWN_REPORT.md`
7. `PHASE2L_LIVE_STRIPE_CREDIT_PACKS_AND_CREDITS_DASHBOARD_REPORT.md`
8. `PHASE2N_ORGANIZATION_CONTEXT_AND_CREDIT_DEBIT_VISIBILITY_REPORT.md`
9. `PHASE2M_ADMIN_BFL_IMAGE_TEST_CREDIT_PRICING_REPORT.md`
10. `PHASE2J_STRIPE_TESTMODE_CREDIT_PACK_CHECKOUT_REPORT.md`
11. `PHASE_ADMIN_CONTROL_PLANE_REPORT.md`
12. `DATA_INVENTORY.md`
13. `docs/DATA_RETENTION_POLICY.md`
14. `workers/auth/CLAUDE.md`
15. `config/release-compat.json`

Historical but no longer current entrypoints:

- `PHASE1_COMPLETION_HANDOFF.md`
- `PHASE2A_ENTRYPOINT.md`

## Commands To Rerun

Before new implementation work:

```bash
git status --short
git log --oneline -10
npm run release:preflight
```

For documentation-only updates:

```bash
git diff --check
npm run check:js
npm run test:release-compat
npm run test:release-plan
npm run validate:release
npm run test:cloudflare-prereqs
npm run validate:cloudflare-prereqs
npm run check:route-policies
npm run check:worker-body-parsers
npm run check:data-lifecycle
npm run check:admin-activity-query-shape
npm run test:operational-readiness
npm run check:operational-readiness
npm run test:workers
npm run test:static
npm run test:asset-version
npm run validate:asset-version
npm run build:static
npm run release:preflight
```

Do not run production deploys, remote D1 migrations, `release:apply`, live Stripe setup, real production billing webhook tests, or Cloudflare dashboard mutations from Codex.

## Recommended Next Work

Recommended next phase: collect main-only verification evidence for migrations through `0053`, the Phase 3.4 member image gateway behavior, the Phase 3.6 member music gateway behavior, the Phase 3.7 replay/finalization/cleanup hardening, the Phase 3.8 member video gateway behavior, the Phase 3.9 AI cost policy baseline/preflight guard, the Phase 4.1/4.2 budget policy design/helper/check output, the Phase 4.3 charged Admin BFL image-test budget metadata, the Phase 4.4/4.8.2/4.10/4.12/4.13/4.15.1/4.16/4.17 budget evidence report, the Phase 4.5 admin video job budget metadata/queue behavior, the Phase 4.6 News Pulse visual budget metadata/status behavior, the Phase 4.7 internal caller-policy guard behavior, the Phase 4.8.1/4.8.2 admin text/embeddings durable idempotency plus cleanup/inspection behavior, Phase 4.9 Admin Music durable metadata-only idempotency behavior, Phase 4.10 Admin Compare durable metadata-only idempotency behavior, Phase 4.12 Admin Live-Agent durable stream-session idempotency behavior, Phase 4.13 sync video debug disabled-by-default behavior, Phase 4.15 Cloudflare master switch behavior, Phase 4.15.1 D1/Admin UI app switch behavior, Phase 4.16 cap-design evidence, and Phase 4.17 `platform_admin_lab_budget` cap-foundation evidence before any live claim. If staging is intentionally skipped, use a bounded operator-run production canary with `ENABLE_ADMIN_STRIPE_TEST_CHECKOUT=true` for Testmode and `ENABLE_LIVE_STRIPE_CREDIT_PACKS=true` only for the shortest possible live credit-pack canary window. Include Phase 2-N organization selection, Phase 2-M/4.3 admin-only BFL image-test charging and budget metadata, member image gateway behavior, member music gateway behavior, member video gateway behavior, admin async video budget metadata/queue behavior, News Pulse visual budget metadata/status behavior, internal caller-policy guard behavior, admin text/embeddings durable idempotency and cleanup/inspection behavior, Admin Music durable metadata-only idempotency behavior, Admin Compare durable metadata-only idempotency behavior, Admin Live-Agent durable stream-session behavior, sync video debug retirement evidence, Admin AI budget switch list/update/effective-state evidence, platform budget cap list/update/usage evidence, `report:ai-budget-evidence`, and `check:ai-cost-policy` output in that evidence set. This is not production-readiness approval.

Priority order:

1. Apply migrations through `0053` in staging/main-only operator flow and verify release compatibility against real resources before auth Worker deploy.
2. Configure Stripe Testmode in staging and verify platform-admin-only checkout, kill-switch failure, webhook signature validation, exact-once credit grant, duplicate delivery, payload mismatch, unpaid/no-credit behavior, non-admin-created checkout no-credit behavior, and Pricing success/cancel states.
3. Verify Admin Control Plane sections against real staging APIs.
4. Verify org/billing/image/text/lifecycle cleanup flows end-to-end in staging.
5. Verify the Phase 2-L Credits page, live checkout authorization, live webhook signature validation, exactly-once grant, no-credit failure paths, and no public pricing exposure.
6. Verify Phase 2-N `/account/organization.html`, active organization selector/defaulting, solo-admin BITBI owner setup, Organization/Credits/Admin AI Lab context sharing, and safe platform-admin-not-owner warning.
7. Verify Phase 2-M/4.3 Admin AI Lab organization selector, BFL cost labels, sufficient-credit denial, provider-failure no-charge behavior, exactly-once debit, safe budget-policy metadata, balance refresh, and sanitized ledger/Credits/Organization dashboard display.
8. Verify Phase 2.3 billing review UI/resolution metadata and Phase 2.4 read-only reconciliation reporting through the Phase 2.6 main-only evidence process if no staging environment exists, without treating either as remediation.
9. Run `npm run check:ai-cost-policy` when changing AI route policy metadata or provider-calling routes; default mode now enforces the Phase 3.9 known-gap baseline and fails on unbaselined provider-cost drift or member gateway regressions.
10. Choose one next implementation track: Phase 4.17 usage-recording repair/reconciliation hardening, the next budget-cap scope, a remaining internal caller-policy gap, refund/chargeback/manual-remediation policy, or tenant-owned asset migration.

## What Not To Redo

Do not redo Phase 0/1 hardening, Phase 2-A org/RBAC, Phase 2-B billing foundations, Phase 2-C/D/E/F image usage enforcement/replay/cleanup, Phase 2-H text generation, Phase 2-I billing event ingestion, Phase 2-J Stripe Testmode checkout, Phase 2-K admin checkout lockdown, Phase 2-L live credit packs/Credits dashboard, Phase 2-M admin BFL image-test charging, Phase 2-N organization context/credit-debit visibility, Phase 2.3 billing review UI, Phase 2.4 read-only reconciliation reporting, Phase 2.6 main-only release evidence tooling, Phase 3.1 AI Cost Gateway inventory/design docs, Phase 3.2 AI Cost Gateway contract/helpers/tests, Phase 3.3 AI Cost Operation Registry/report-only baseline, Phase 3.4 member personal image gateway pilot, Phase 3.6 member music gateway migration, Phase 3.7 member image/music hardening, Phase 3.8 member video gateway migration, Phase 3.9 AI cost policy guard/baseline, Phase 4.1 admin/platform budget policy design, Phase 4.2 admin/platform budget helper contracts/tests, Phase 4.3 charged Admin BFL image-test budget metadata hardening, Phase 4.4 read-only Admin/Platform AI budget evidence reporting, Phase 4.5 admin async video job budget enforcement, Phase 4.6 OpenClaw/News Pulse visual budget controls, Phase 4.7 internal AI Worker caller-policy guard, Phase 4.8 admin text/embeddings budget metadata, Phase 4.8.1 admin text/embeddings durable idempotency, Phase 4.8.2 admin usage-attempt cleanup/inspection, Phase 4.9 Admin Music budget enforcement, Phase 4.10 Admin Compare budget enforcement, Phase 4.11 Admin Live-Agent audit/design prep, Phase 4.12 Admin Live-Agent budget enforcement, Phase 4.13 sync video debug retirement decision, Phase 4.14 Admin Image branch handling, Phase 4.15 runtime budget switch enforcement, Phase 4.15.1 D1 Admin UI budget switch control, Phase 4.16 live cap design/evidence, Phase 4.17 first `platform_admin_lab_budget` cap foundation, Admin Control Plane, or Pricing unless a focused regression is found.

## Commit Guidance

Commit Phase 4.16 live platform budget cap design/evidence, registry/check metadata, tests, and documentation together after review. No deployment, remote migration, live Stripe setup, real payment, real AI provider call, credit clawback, unrelated route migration, member route migration, runtime cap enforcement, route behavior change, or Cloudflare dashboard mutation was performed by Codex.
